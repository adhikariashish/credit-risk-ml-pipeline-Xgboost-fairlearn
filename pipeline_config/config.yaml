data:
  raw_dir: "data/raw"
  interim_dir: "data/interim"
  processed_dir: "data/processed"

features:
  target: "default_flag"
  sensitive:
    - "property_state"

model:
  random_seed: 42
  xgboost:
    n_estimators: 100
    max_depth: 6
    learning_rate: 0.1
